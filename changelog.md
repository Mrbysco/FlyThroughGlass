* Add a block tag `flythroughglass:blocks_to_break`
* Add a config
* Add ability to run through blocks (if enabled in config)
* Allow configuring the minimum speed a player must have to run through blocks